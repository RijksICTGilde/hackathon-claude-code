# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://docs.github.com/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file

version: 2
updates:
  - package-ecosystem: "docker" # See documentation for possible values
    directory: "/claude-sandbox" # Dockerfile lives here, not at repo root
    schedule:
      interval: "weekly"
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
  # Het sample-project waarmee de podman-set wordt gerookttest. Zonder deze
  # entry krijgt het alleen security-updates; version-updates blijven uit,
  # waardoor de smoke-test op verouderde afhankelijkheden blijft draaien.
  - package-ecosystem: "maven"
    directory: "/claude-sandbox/podman/sample"
    schedule:
      interval: "weekly"
